name: Notify
on:
  workflow_run:
    workflows: ["Update summary", "Check endpoint status"]
    types:
      - completed

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Send Notification
        run: echo "Sending notification for {{ status }}"
